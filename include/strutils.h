// Devin Driggs
// String utilities utilized in my C++ compiler
// Mostly copied/derived from stackoverflow answers

// Returns true if str starts with prefix, else false
bool startsWith(char* str, char* prefix);

// returns true if str ends with suffix, else false
bool endsWith(char* str, char* suffix);

// Returns true if filename has a file extension, else false
bool hasExtention(char* filename);